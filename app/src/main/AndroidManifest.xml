<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.icecream.cc.ilovewords" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/i_iove_words_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" >
        </activity>
        <activity android:name=".ShowMyWordsActivity" >
        </activity>
        <activity android:name=".UserPanelActivity" >
        </activity>
        <activity android:name=".WordbankActivity" >
        </activity>
        <activity android:name=".ShowWordDetailActivity" >
        </activity>
        <activity android:name=".AdministratorActivity" >
        </activity>
        <activity android:name=".AdminLoginActivity" >
        </activity>
        <activity android:name=".AdminInsertWordActivity" >
        </activity>
        <activity android:name=".AdminViewLoginLogActivity" >
        </activity>
    </application>

</manifest>
